{
  "name": "randome",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon ./bin/www",
    "dev": "concurrently \"nodemon ./bin/www\" \"cd client && npm start\"",
    "prod": "cd client && npm run build && cd .. &&set NODE_ENV=production&& node ./bin/www",
    "lint": "eslint src/ --fix"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "dotenv": "^8.0.0",
    "express": "~4.16.0",
    "firebase": "^6.3.1",
    "firebase-admin": "^8.3.0",
    "mongodb": "^3.3.0-beta2",
    "mongoose": "^5.6.7",
    "morgan": "~1.9.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "eslint": "^6.1.0",
    "nodemon": "^1.19.1"
  }
}
